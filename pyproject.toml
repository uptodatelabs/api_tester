[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "apiprobe"
version = "1.0.1"
description = "API testing tool with a Textual TUI"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "textual",
    "httpx",
    "pytest",
]

[project.scripts]
apiprobe = "api_tester.app:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
